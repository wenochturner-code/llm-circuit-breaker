{
  "name": "llm-execution-guard",
  "version": "0.2.0",
  "description": "Minimal control-plane primitive for limiting LLM execution costs.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
   "scripts": {
  "build": "tsc",
  "test": "tsx --test test/*.test.ts",
  "prepublishOnly": "npm run test && npm run build"
},

  "devDependencies": {
    "tsx": "^4.21.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
